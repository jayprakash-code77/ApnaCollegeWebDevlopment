<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Content</title>
</head>
<body>
    <div>
        <p><%= data.id %></p>
        <p><%= data.username %></p>
        <form  method="post" action="/posts/<%= data.id %>?_method=PATCH">
            <label for="newContent">Type your new Content</label> <br>
            <textarea name="content" id=""><%= data.content %></textarea><br>
            <button name="submit" type="submit">Save</button>
        </form>
    </div>
    
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Content</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            border: 1px solid #3498db;
            border-radius: 10px;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 400px;
        }
        .container p {
            color: #34495e;
            line-height: 1.6;
            text-align: center;
        }
        .container label {
            font-weight: bold;
            color: #2c3e50;
            display: block;
            margin-bottom: 10px;
        }
        .container textarea {
            width: 100%;
            height: 100px;
            border: 1px solid #3498db;
            border-radius: 5px;
            padding: 10px;
            margin-top: 10px;
            margin-bottom: 20px;
            resize: none;
            box-sizing: border-box;
        }
        .container button {
            display: block;
            width: 100%;
            padding: 10px;
            background-color: #3498db;
            color: white;
            border: none;
            border-radius: 5px;
            text-align: center;
            cursor: pointer;
            font-size: 14px;
        }
        .container button:hover {
            background-color: #2980b9;
        }
    </style>
</head>
<body>
    <div class="container">
        <p><%= data.id %></p>
        <p><%= data.username %></p>
        <form method="post" action="/posts/<%= data.id %>?_method=PATCH">
            <label for="newContent">Type your new Content</label><br>
            <textarea name="content" id="newContent"><%= data.content %></textarea><br>
            <button name="submit" type="submit">Save</button>
        </form>
    </div>
</body>
</html>
